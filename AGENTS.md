# Customer Agent 2 项目协作规则

## 1. 项目定位

Customer Agent 2 是一个面向开源展示与求职简历的大模型应用开发项目。项目使用 Python 实现一条可解释、可测试、可评测的 RAG 主链路，重点展示大模型应用工程能力，而不是普通后台 CRUD。

项目参考开源项目 [nageoffer/ragent](https://github.com/nageoffer/ragent) 的设计思想，但不按 Java 文件逐个翻译。迁移时优先保留问题、机制、取舍和可验证行为，并按照 Python 生态重新设计实现。

## 2. 事实来源优先级

讨论“当前项目如何实现”时，按以下顺序判断：

1. 当前仓库源码和自动化测试。
2. `docs/PROJECT_SCOPE.md`、`docs/ARCHITECTURE.md` 和已接受的 ADR。
3. Ragent 参考项目源码。
4. 框架文档和通用工程知识。

源码不存在的能力必须明确标记为规划项，不得把 README 或设计文档中的计划描述成已实现功能。涉及参考项目时应区分：

- `VERIFIED FROM SOURCE`：当前源码可以直接证明。
- `INFERENCE`：根据源码作出的合理推断。
- `GENERAL KNOWLEDGE`：通用知识，不代表当前项目已经采用。
- `NOT FOUND`：没有找到足够证据。

## 3. 当前范围

P0/P1 范围以 `docs/PROJECT_SCOPE.md` 为唯一事实来源。当前重点包括：

- 文档解析、分块、Embedding 与向量索引。
- 会话记忆、问题重写、多问题拆分、意图识别和歧义澄清。
- 向量召回、去重、RRF、Rerank、TopK 和引用来源。
- Prompt 组装、LLM 流式回答、SSE、超时与取消。
- 固定评测集、失败样本和 Rerank ON/OFF 对比。

首版不主动开发完整前端、企业级权限、多租户、管理后台、Kubernetes 和复杂 Agent 自主循环。MCP、VLM、关键词检索和知识图谱按开发计划分阶段加入。

## 4. 架构约束

- 在线 RAG 使用显式、可调试的异步 Pipeline，不将核心流程隐藏在高层黑盒 Chain 中。
- 业务编排与供应商 SDK、数据库、向量库、对象存储相互隔离。
- Chat、Embedding、Rerank、VLM、SearchChannel 和文档解析器必须通过清晰接口或 Protocol 解耦。
- 核心领域对象使用类型注解和 Pydantic/Dataclass 表达，不在模块间传递无约束字典。
- 多通道检索使用结构化并发；单通道失败可降级，但不得吞掉错误事实。
- 流式任务必须支持超时、客户端断开和资源释放。
- LangChain/LlamaIndex 不作为主链路骨架。只有在确实需要持久化 ReAct Agent 时才评估 LangGraph，并通过 ADR 记录决策。

## 5. 模型与检索约束

- 模型 ID、Base URL、超时、额度和能力全部由配置注入，禁止硬编码。
- 默认最终回答模型与内部快速模型分开配置。
- 当前本地 Embedding Baseline 为 `BAAI/bge-base-zh-v1.5`，768 维、最大 512 Token；最终模型必须以实验结果决定。
- 知识库或索引必须记录 Embedding 模型、维度和版本。修改维度意味着重建索引，不能静默切换。
- 向量默认归一化并使用 Cosine 相似度；任何变化必须有评测依据。
- Rerank 使用专用排序模型；不可默认用 Chat 模型替代。
- 检索参数必须区分召回预算、Rerank 候选数和最终上下文 TopK。

## 6. 数据、密钥和版权

- 严禁提交真实 `.env`、API Key、数据库密码、Token、Cookie 或带凭据的 URL。
- 只允许提交 `.env.example`；日志和异常中不得输出完整密钥。
- 不提交模型权重、原始测试文档、数据库卷和个人数据。
- 引用或移植第三方代码前必须核对许可证，并在 `NOTICE` 中维护归属。
- 评测集、上游宣传数据和本项目复现数据必须明确区分，不得把上游数字写成本项目成果。

## 7. Python 工程规范

- 目标运行时为 Python 3.11。
- 依赖统一维护在 `pyproject.toml`，使用锁文件保证可复现。
- 新增公共函数、接口和领域模型必须提供完整类型注解。
- 禁止无边界的 `except Exception: pass`；降级必须记录结构化原因。
- 配置使用 Pydantic Settings，数据库变更使用 Alembic。
- 日志使用结构化字段，禁止拼接敏感信息。
- 优先保持模块职责单一，避免循环依赖和全局可变状态。

## 8. 测试与评测

- 新功能至少包含对应单元测试；跨数据库、Redis、模型 HTTP 的关键路径应有集成测试或可控 Mock。
- Bug 修复必须先补充能够复现问题的测试。
- 评测一次只改变一个变量，必须记录 Baseline、Variant、控制变量、数据集、指标和失败样本。
- P0 评测优先级：Intent Top-1、Rerank ON/OFF、Retrieval Hit@K/MRR、端到端延迟。
- 不得编造模型效果、准确率、延迟或成本数字。

## 9. 变更策略

以下变更必须先更新或新增 ADR：

- 更换主 Web 框架、ORM、向量数据库或任务队列。
- 更换默认 Embedding 模型或向量维度。
- 引入 LangGraph、LangChain 或新的核心编排方式。
- 修改公开 API、SSE 事件协议或数据库核心表结构。
- 引入新的外部基础设施或产生明显成本的云服务。

修改前先检查工作区状态，保留用户已有改动。不得擅自重置、覆盖或删除不属于当前任务的内容。

## 10. 完成标准

功能只有在以下条件全部满足后才能标记为完成：

- 实现与 `PROJECT_SCOPE` 一致。
- 正常路径、失败路径和降级路径均有验证。
- 测试通过，且没有泄露敏感信息。
- 必要的配置、迁移和文档同步更新。
- 能说明设计原因、替代方案、局限和实验验证方式。
- Git 差异中不包含缓存、模型权重、真实数据或无关修改。
